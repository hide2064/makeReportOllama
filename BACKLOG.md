# 次フェーズ作業候補（バックログ）

最終更新: 2026-03-27

---

## 優先度高

| # | 項目 | 内容 |
|---|------|------|
| H-1 | ドラッグ＆ドロップファイルアップロード | ボタン式をドロップゾーン化。ファイルをドラッグして離すだけでセット。 |
| H-2 | アップロードデータプレビュー | CSV/Excel アップロード後に先頭数行をテーブル表示＋必須列チェックを UI 側で即時フィードバック。 |
| H-3 | Ollama モデル選択 UI | `GET /api/models`（Ollama `/api/tags` 中継）でローカルモデル一覧を取得し、Analyst/Writer をドロップダウンで切り替え可能にする。 |
| H-4 | 生成済みレポート履歴 | 直近 N 件を保持し `GET /api/history` で一覧。フロントで「過去の生成結果」セクションからワンクリック再ダウンロード。 |
| H-5 | 分析期間フィルター | フォームに「開始日〜終了日」を追加し、`excel_reader.py` でその範囲に絞り込んでから集計する。 |

---

## 優先度中

| # | 項目 | 内容 |
|---|------|------|
| M-1 | ブラウザ内グラフ表示 | `GET /api/charts` で集計 JSON を返し、`recharts` 等でブラウザ内にもグラフを描画。PPTX 未ダウンロードでも結果確認可能。 |
| M-2 | SSE によるリアルタイムストリーミング | `/api/progress/stream` を `text/event-stream` で実装しポーリングを廃止。 |
| M-3 | 列名マッピング UI | 英語列名など必須列名が異なるファイルに対応。ドロップダウンで「どの列を"日付"として使うか」をユーザーが指定。 |
| M-4 | カスタムコンテキスト入力欄 | フォームに「追加コンテキスト」テキストエリアを追加し `raw_summary` に追記して AI に渡す。 |
| M-5 | 目標値（KPI）入力と達成率比較 | 月次目標売上を入力し、達成率をグラフスライドに重ねて表示。Writer プロンプトにも目標比較として渡す。 |
| M-6 | PDF 出力オプション | LibreOffice CLI 等で PPTX → PDF 変換。フォームにラジオボタン「PPTX / PDF」を追加。 |
| M-7 | RAG コンテキストデバッグパネル | 最後の RAG 検索で使ったチャンク（スライド元・類似度）を UI で表示し AI の根拠を可視化。 |

---

## 優先度低

| # | 項目 | 内容 |
|---|------|------|
| L-1 | ダークモード | CSS カスタムプロパティ + トグル。`prefers-color-scheme` 連動。 |
| L-2 | Docker コンテナ化 | `docker-compose.yml` で backend + frontend を一括起動。別 PC・本番環境への展開を容易化。 |
| L-3 | ブラウザ通知 | 生成完了時に `Notification API` でデスクトップ通知。長時間処理中に他作業しながら待てる。 |
| L-4 | E2E テスト（Playwright） | ファイルアップロード→モック API→ダウンロードボタン表示の一連フローを自動テスト化。 |
| L-5 | GitHub Actions CI | push 時に `pytest` + `vitest --run` を自動実行するワークフロー追加。 |

---

## 実装済み（参考）

- Phase 1: Excel 読み込み → PPTX テンプレート置換
- Phase 2: 2 モデルパイプライン（Analyst: qwen2.5:3b / Writer: qwen3:8b）
- Phase 3: RAG（ChromaDB + nomic-embed-text）+ 過去資料管理 UI
- Phase 4a: 商品別四半期売上表スライド（python-pptx テーブル）
- Phase 4b: 月次推移バーチャート + 商品別横棒/円グラフスライド（matplotlib）
- 高優先度改善: UUID 出力ファイル管理、Analyst リトライ（最大3回）、CSV サポート、利益データ列対応
- 中優先度改善: GET /api/templates、スライド構成チェックボックス、グラフ種別ラジオ、地域/担当者別表スライド、YoY 前年同期比、汎用 `_add_table_slide`
- 低優先度改善: .gitignore 整備、LOG_LEVEL 環境変数化、pptx_generator ユニットテスト拡充
